/* Controlador de Fluxo 
     - IF(Se) -> Faça Isso
     - Else(Se não) -> Executa o Else

    Operadores de comparação

    > maior que
    < menor que
    == igual que
*/ 

const senha = 1234567 
const senhaDigitada = 1234567

if(senha == senhaDigitada) {
    console.log('Seja bem-vindo vc foi logado')
}else {
    console.log('Senha errada')
}
